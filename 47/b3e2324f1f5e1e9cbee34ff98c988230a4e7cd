v1:
	* Edit commit message

RFC v2:
	* Split all the mlxsw_sp_port_pvid_set() changes into a different
	  patch
	* Use 'u16 ethtype' and then translate it to et_vlan instead of
	  using magic numbers
	* Set et_vlan as part of mlxsw_reg_spvid_pack()
