v5: *Remove check for a non-null variable.

v4: *Remove init to 0 fields that are already zeroed.

v3: *Add switch_id and len to attrs.
    *Use {} notation instead of {0}.

v2: *Change patch loaction on patchset.
