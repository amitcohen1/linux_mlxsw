v3: *Reword commit message.
    *Update the function docs.
    *Adjust all the devlink_port_attrs_set() usages.

v2: *Split the patchset into two- one for width and one for splittable
     exposing.
    *Put out width attribute only if width is not 0.
